<view wx:if="{{mount}}" class="ad-banner {{ready ? 'is-on' : ''}}">
  <ad
    unit-id="{{unitId}}"
    ad-type="banner"
    ad-theme="{{adTheme}}"
    bindload="onLoadAd"
    binderror="onErrorAd"
  />
</view>

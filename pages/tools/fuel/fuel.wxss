@import "../tool-common.wxss";
@import "../calc-kit.wxss";

.conv-hero {
  background:
    linear-gradient(160deg, rgba(6, 182, 212, 0.16) 0%, var(--color-hero-fade) 58%),
    var(--color-surface);
}

.tool-icon-fuel {
  background: linear-gradient(135deg, #cffafe, #22d3ee);
}

.seg-row + .seg-row {
  margin-top: 12rpx;
}
